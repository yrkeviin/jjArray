//reverse

//ex. 01

//sequência 1 ao 5.
let crescente = [1, 2, 3, 4, 5];

//apliquei o reverse, para deixar na ordem decrescente.
let decrescente = crescente.reverse();

//exibi a let que inverteu a ordem dos elementtos no array.
console.log(decrescente);

//ex. 02

//let do a b c d
let ordemAlfabetica = ['a', 'b', 'c', 'd'];

//inverti a sequência do a b c d e deixei ao contrário, d c b a.
let alfabeticaInvertida = ordemAlfabetica.reverse();

//exibi.
console.log(alfabeticaInvertida);

//O método reverse() inverte os itens de um array. O primeiro elemento do array se torna o último e o último se torna o primeiro.